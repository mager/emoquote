import { robotoBold } from "../../utils/fonts";

const Logo = () => {
  return (
    <div className={`${robotoBold.className} text-3xl`}>
      <span>emoqu</span>
      <span>😎</span>
      <span>te</span>
    </div>
  );
};

export default Logo;
